<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Gyroown.Controls.FavoritesPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="SmallIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid RowDefinitions="Auto,Auto,*">
        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="12,8,12,4">
            <FontIcon Grid.Column="0" Glyph="&#xE734;" FontSize="14"
                      Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                      VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBlock Grid.Column="1" x:Name="HeaderText" FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <Button Grid.Column="2" x:Name="ToggleBtn" Click="OnToggleExpand"
                    Style="{StaticResource SmallIconButtonStyle}" Padding="4,2"
                    AutomationProperties.Name="Expand/Collapse">
                <FontIcon x:Name="ToggleIcon" Glyph="&#xE70D;" FontSize="12" />
            </Button>
        </Grid>

        <!-- Divider -->
        <Border Grid.Row="1" Height="1"
                Background="{ThemeResource CardStrokeColorDefaultBrush}"
                Margin="12,0,12,4" />

        <!-- Favorites content -->
        <ScrollViewer Grid.Row="2" x:Name="ContentScroller"
                      VerticalScrollBarVisibility="Auto"
                      Padding="0,0,0,4">
            <StackPanel x:Name="FavoritesContainer" Spacing="2" />
        </ScrollViewer>
    </Grid>
</UserControl>
