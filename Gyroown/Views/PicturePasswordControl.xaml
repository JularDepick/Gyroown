<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Gyroown.Views.PicturePasswordControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="HintText" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
        <Grid x:Name="Container" MinWidth="400" MinHeight="300" MaxWidth="600" MaxHeight="450"
              HorizontalAlignment="Center" PointerPressed="OnPress" Background="Transparent">
            <Image x:Name="Image" Stretch="Uniform" />
            <Canvas x:Name="MarkCanvas" />
        </Grid>
        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
            <Button x:Name="ResetBtn" Click="OnReset" />
            <Button x:Name="SelectBtn" Click="OnSelect" />
        </StackPanel>
        <TextBlock x:Name="CountText" Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
