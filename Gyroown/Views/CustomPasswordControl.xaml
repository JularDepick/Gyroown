<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Gyroown.Views.CustomPasswordControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="320">
        <TextBlock x:Name="HintText" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
        <PasswordBox x:Name="Input" MaxLength="32" PasswordChanged="OnChanged" KeyDown="OnKey" />
        <CheckBox x:Name="ShowCb" Checked="OnShow" Unchecked="OnShow" />
        <TextBlock x:Name="RangeText" Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
        <TextBlock x:Name="ErrorText" Foreground="{ThemeResource SystemErrorTextColor}"
                   Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Visibility="Collapsed" />
    </StackPanel>
</UserControl>
